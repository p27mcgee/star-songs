curl -vv http://localhost:8086/v1/songs

curl -vv http://localhost:8086/v1/artists

// should return conflict 409 because url and body specify different IDs
curl -vv -X PUT http://localhost:8086/v1/songs/3 \
    -H "Content-Type:application/json" \
    -d "{\"id\":2,\"title\":\"Why Does the Sun Really Shine?\",\"artistId\":2,\"releaseDate\":null,\"url\":null,\"distance\":null}"
